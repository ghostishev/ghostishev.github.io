# Портфоліо — Олена Чумаченко

Статичний односторінковий сайт (HTML + CSS + JS, без збірки та залежностей),
зверстаний за слайдами презентації-портфоліо.

```
index.html    розмітка та весь текст
styles.css    палітра, типографіка, сітки, анімації
script.js     індикатор прокрутки, шапка, поява блоків, паралакс
images/       фото та сертифікати (див. images/README.md)
.nojekyll     GitHub Pages віддає файли як є, без Jekyll
```

## Що всередині

- **Перший екран** — велике слово `ПОРТФОЛІО`, літери «ФО» контурні, щоб фото
  на прозорому фоні лягало поверх них, як на слайді.
- **Секції** `Про мене → Освіта → Досвід і ніші → Роботи → Контакти`
  з нумерацією `[ 01 ]`, як у презентації.
- **Роботи** — три кейси зі слайдів 05–09 презентації: магазин бігового
  спорядження, онлайн-школа англійської, особистий і книжковий блог.
  Дев'ять плиток, кожна відкриває карусель на весь екран.
- **Контакти** повторюють фінальний слайд «Готова до співпраці»: постать
  із прозорим фоном стоїть на нижньому краї темного блока поруч із CTA.
- Шапка з навігацією з'являється при прокрутці, підсвічує активну секцію.
- Блоки плавно з'являються (`IntersectionObserver`), картки ніш мають
  hover-заливку.
- Адаптив від 320 px; повага до `prefers-reduced-motion`.

## Зображення

Усі на місці, опис — в [images/README.md](images/README.md). Оригінали
лежать в `images/source/`. Якщо якийсь файл зникне, блок сховається або
покаже заглушку — сторінка не зламається.

Сертифікати й каруселі відкриваються на весь екран по кліку. Всередині
каруселі — стрілки, `←`/`→` та свайп; `Esc` або клік по тлу закривають.

Слайди каруселей — у `images/works/<кейс>/01.jpg…`; список файлів скрипт
збирає з атрибутів `data-gallery` і `data-count` на плитці.

## Локальний перегляд

```bash
python3 -m http.server 8000
```

Далі відкрити http://localhost:8000

## Публікація на GitHub Pages

Репозиторій: `ghostishev/ghostishev.github.io` → сайт публікується на
**https://ghostishev.github.io**

Увімкнути один раз: **Settings → Pages → Build and deployment → Source:
*Deploy from a branch*** → бранч `main`, папка `/ (root)` → **Save**.
Далі кожен `git push` у `main` автоматично оновлює сайт (30–60 секунд).

## Що варто оновити перед публікацією

- Telegram-нік у трьох місцях `index.html` (шапка, картка CTA, секція контактів),
  якщо він відрізняється.
- `og:image` в `<head>` — після додавання `images/hero.png`.

## Як додати нову роботу

1. Стиснути слайди: довша сторона 1080 px, JPEG q72 — і покласти в
   `images/works/<новий-кейс>/` як `01.jpg`, `02.jpg`, …
2. Обкладинку продублювати як `cover.jpg` (800 px).
3. Скопіювати `<figure class="work …>` у потрібному кейсі `index.html`
   і виправити `data-gallery`, `data-count`, `src`, `alt` і підпис.
